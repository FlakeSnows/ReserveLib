# Требования к приложению:
#### Минимальные критерии работоспособности
Сборка и запуск
- [x] Приложение собирается без ошибок.
- [x] Секреты не указаны в коде, выбран надежный способ их подтягивать. (Через docker-compose environment).

Есть README с:
- [x] требованиями (Java версия, Maven/Gradle, Docker)
- [x] запуск локально средствами Java или в контейнере
- [x] информацией, где хранить секреты
- [x] описанием контракта
- [x] портами и учетными данными для демо.
- [x] Поднята бд.

Здоровье сервиса
- [x] Healthcheck приложения. (Spring Actuator подключен).
- [x] Отдельно виден статус соединения с БД или кидается понятная ошибка при недоступной БД.

Миграции и схема БД
- [x] бд заполняется приложением без ручного создания таблиц. Схема описана миграциями (Flyway) и накатывается автоматически при старте.
- [x] Есть внешние ключи/constraint там, где нужно по предметной области.

Сквозные пользовательские сценарии (2–3 ключевых)
- [x] Для каждого сценария есть последовательность REST-запросов (создание → чтение → обновление/удаление).
- [x] Используются корректные методы и статусы: POST 201, GET 200, PUT/PATCH 200/204, DELETE 204.

Валидация и обработка ошибок
- [x] Неверные данные → 400 с понятным телом ошибки. (Bean Validation + GlobalExceptionHandler).
- [x] Отсутствующие ресурсы → 404, невалидная аутентификация/доступ → 401/403 (если есть авторизация).
- [x] Нет 500 на предсказуемых ошибках пользователя.

Сохранность данных
- [x] После перезапуска контейнера/приложения ранее созданные сущности сохраняются. (Volume в Docker настроен).

Логи и наблюдаемость
- [x] Логи содержат время, уровень, краткое описание события/ошибки. (SLF4J добавлен в BookCatalogService).
- [x] Уровень логирования настраивается в конфигурации приложения. (Настройки в application.properties).

Рекомендуемые (повышенный уровень, за дополнительные баллы)
- [x] Документация API: Swagger/OpenAPI доступен по /swagger-ui или есть актуальная Postman-коллекция с примерами.

Минимальные тесты:
- [x] 3–5 unit-тестов ключевой бизнес-логики. (4 теста в BookCatalogServiceTest).
- [x] 1–2 интеграционных теста с БД (H2/Testcontainers). (2 теста в TestLibReserveTests).
- [ ] Простая авторизация: регистрация/логин и защищенный эндпоинт, который без токена/сессии возвращает 401/403.
- [x] Для списков есть базовая фильтрация/пагинация или лимит.
- [ ] Короткая демо-роль: предзаполненный «демо-пользователь» для быстрого входа.